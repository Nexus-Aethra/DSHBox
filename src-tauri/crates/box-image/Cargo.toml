[package]
name = "box-image"
version = "0.1.0"
edition = "2021"

[dependencies]
box-extensions = { path = "../box-extensions" }
box-install-handlers = { path = "../box-install-handlers" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tar = "0.4"
flate2 = "1.1"
