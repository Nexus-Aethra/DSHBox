[package]
name = "box-toolchains"
version = "0.1.0"
edition = "2021"

[dependencies]
box-foundation = { path = "../box-foundation" }
box-runtime = { path = "../box-runtime" }
serde = { version = "1.0", features = ["derive"] }
