[package]
name = "box-client"
version = "0.1.0"
edition = "2021"

[dependencies]
box-foundation = { path = "../box-foundation" }
box-server-core = { path = "../box-server-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
